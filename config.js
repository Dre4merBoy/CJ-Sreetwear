// Configuración pública para el cliente.
// En local, server.js genera este archivo dinámicamente desde .env
// (ruta /config.js). En hosting estático (Vercel) se sirve este archivo.
// ⚠️ Solo valores PÚBLICOS — nunca secretos. Actualiza el número real de
// WhatsApp aquí (formato internacional sin "+", ej. 52XXXXXXXXXX).
window.SALMAS_CONFIG = { "whatsapp": "521234567890" };
